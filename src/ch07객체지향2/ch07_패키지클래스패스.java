package ch07객체지향2;

public class ch07_패키지클래스패스 {
    // 서로 관련된 클래스의 묶음
    // 즉 패키지 == 클래스를 묶어놓은것.
    // 클래스는 클래스 파일(*.class), 패캐지는 폴더. 하위 패키지는 하위폴더.
    // 클래스의 실제 이름(full nave)은 패키지를 포함(java.lang.string)
    // rt.jar는 클래스들을 압축한 파일(JDK설치경로 JRE LIB에 위치)
    // ㄴ 리얼타임. JAR 클래스파일 묶음.
    // rt.jar 파일이 너무커서 java 9부터는 모듈로 나뉘어놓음.

    // 패키지의 선언.
    // 패키지는 소스파일의 첫 번째 문장으로 단 한번 선언
    // 패키지 선언이 없으면 이름없는 패키지에 속하게 됨.

}
